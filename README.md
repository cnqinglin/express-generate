# 使用方法

```
  git clone git@github.com:cnqinglin/express-generate.git <你的目录>
  cd <你的目录名>
  yarn install 
  yarn global add ts-node
  node app.js  or node app2.ts   // 执行的入口文件
```

# 上传到其他仓库：

```
  git remote set-url origin <新仓库的地址>
  git push -u origin master
```
